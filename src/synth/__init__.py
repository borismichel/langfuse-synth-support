"""Support Triage Deflection — a Demo Depot synth kit (walking skeleton).

The runtime `synth` CLI (`seed` + `verify`), each wired through the shared library
(`langfuse-synth-core`). Generation is deterministic and model-free, so the seed passes
the determinism golden gate offline. Grow this into the real story; keep the gate green.
"""
